spring.datasource.url=jdbc:postgresql://localhost:5432/prod_catalog
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:postgres}

spring.jpa.generate-ddl=false
spring.jpa.show-sql=false
spring.jpa.hibernate.ddl-auto=validate

#s3.amazon.bucket=permishin
#
#upload.path=https://productcat.s3.us-east-2.amazonaws.com
#
#aws.access.key=${?AWS_ACCESS_KEY}
#aws.secret.key=${?AWS_SECRET_KEY}

upload.Dir=src/main/resources/static/images
file.Dir=/static/images
spring.servlet.multipart.max-file-size = 5MB
spring.servlet.multipart.max-request-size = 5MB



